<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".view.MainActivity">
    <item
        android:id="@+id/app_scheduler_menu"
        android:icon="@drawable/ic_menu_icon"
        android:title=""
        android:visible="true"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/appHistory"
                android:title="@string/appSchedulerHistoryTitle"
                android:visible="true" />
            <item
                android:id="@+id/deleteAll"
                android:title="@string/deleteAllSchedule"
                android:visible="true" />
            <item
                android:id="@+id/appHistoryDeleteAll"
                android:title="@string/appSchedulerHistoryDelete"
                android:visible="true" />

        </menu>
    </item>
</menu>